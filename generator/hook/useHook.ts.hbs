import { useState } from 'react'

type {{pascalCase hook}}Type = {
  // {{pascalCase hook}}Type
}

export const use{{pascalCase hook}} = ({}: {{pascalCase hook}}Type) => {
  const [state, setState] = useState(null)

  return { state }
}
